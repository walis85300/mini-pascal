Program hola;
Var
	j,i: Integer;
	b: Boolean;
function Integer dolares(tasasimadi: Boolean)
	begin
		read bolivares;
		if (tasasimadi) or ( t <> 2) then
			salida := bolivares / 300;
		else
			salida := bolivares / 1100;
		end;
		
		for variable := 0 to 20 do
			begin 
				write(pupu);
			end;
		while (a=0) or (b<3) do
			begin
				write(4);
			end;
	end;
begin
	dolar := 800;
	t := dolares();
	write(dolar);
end;
